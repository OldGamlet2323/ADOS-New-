# Environment variables for the backend Docker Compose and local development.
# IMPORTANT: This file contains secrets (DB password). Do NOT commit this file to public repos.
# Commit a .env.example with non-sensitive defaults instead.

# PostgreSQL (used by docker-compose)
DB_NAME=restore
DB_USER=postgres
DB_PASSWORD=password
POSTGRES_VERSION=15-alpine

# If you want the DB to be reachable from your host (for running the app in IntelliJ),
# compose can publish the port 5432 (see docker-compose). When published, your
# application can use this DB URL from the host:
DB_URL="jdbc:postgresql://localhost:5430/${DB_NAME}"

# Backend image and runtime
BACKEND_IMAGE=mitit/minauthproj:latest
SERVER_PORT=8080

# Volumes / data
PGDATA_VOLUME=pgdata
UPLOADS_DIR=./uploads

# Helpful runtime flags (optional)
# JVM_OPTS=-Xms256m -Xmx512m